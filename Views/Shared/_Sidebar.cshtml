@{
    // You can pass active menu from ViewData or Controller
    string activeMenu = ViewData["ActiveMenu"]?.ToString() ?? "Dashboard";
    string activeSubMenu = ViewData["ActiveSubMenu"]?.ToString() ?? "";
}

<link href="~/Content/css/sidebar.css" rel="stylesheet" />

<aside class="sidebar" id="sidebar">
    <!-- Logo -->
    <div class="logo">
        <i class="fas fa-graduation-cap"></i>
        <span>Registrar ERP</span>
    </div>

    <!-- Menu -->
    <ul class="menu-tree" id="menuTree">

        <!-- ============================================================ -->
        <!-- DASHBOARD -->
        <!-- ============================================================ -->
        <li>
            <div class="parent @(activeMenu == "Dashboard" ? "active" : "")" data-target="">
                <i class="fas fa-th-large"></i>
                Dashboard
                <span class="badge-dot"></span>
            </div>
        </li>

        <!-- ============================================================ -->
        <!-- UNIVERSITY SETUP (Main Menu) -->
        <!-- ============================================================ -->
        <li>
            <div class="parent @(activeMenu == "University" ? "active" : "")" data-target="subUniversity">
                <i class="fas fa-university"></i>
                University
                <span class="arrow"><i class="fas fa-chevron-down"></i></span>
            </div>
            <ul class="submenu @(activeMenu == "University" ? "open" : "")" id="subUniversity">
                <!-- University Profile -->
                <li>
                    <a href="@Url.Action("UniversityProfile", "University")"
                       class="@(activeSubMenu == "Profile" ? "active-link" : "")">
                        <i class="fas fa-building-columns"></i> University Profile
                    </a>
                </li>
                <!-- Campus Management -->
                <li>
                    <a  href="@Url.Action("CampusManagement", "University")"
                       class="@(activeSubMenu == "Campus" ? "active-link" : "")"
                       asp-controller="University" asp-action="Campus">
                        <i class="fas fa-flag"></i> Campus Management
                    </a>
                </li>
               
            </ul>
        </li>

        <!-- ============================================================ -->
        <!-- ACADEMIC SETUP (Main Menu) -->
        <!-- ============================================================ -->
        <li>
            <div class="parent @(activeMenu == "Academic" ? "active" : "")" data-target="subAcademic">
                <i class="fas fa-book-open"></i>
                Academic Setup
                <span class="arrow"><i class="fas fa-chevron-down"></i></span>
            </div>
            <ul class="submenu @(activeMenu == "Academic" ? "open" : "")" id="subAcademic">
                <!-- Academic Session -->
                <li>
                    <a href="@Url.Action("AcademicSession", "AcademicSetup")"
                       class="@(activeSubMenu == "Session" ? "active-link" : "")">
                        <i class="fas fa-calendar-alt"></i> Academic Session
                    </a>
                </li>
                <!-- Faculty -->
                <li>
                    <a href="@Url.Action("FacultyManagement", "University")"
                       class="@(activeSubMenu == "Faculty" ? "active-link" : "")"
                       asp-controller="Academic" asp-action="Faculty">
                        <i class="fas fa-layer-group"></i> Faculty
                    </a>
                </li>
                <!-- Department -->
                <li>
                    <a href="@Url.Action("DepartmentManagement", "AcademicSetup")"
                       class="@(activeSubMenu == "Department" ? "active-link" : "")"
                       asp-controller="Academic" asp-action="Department">
                        <i class="fas fa-sitemap"></i> Department
                    </a>
                </li>
                <!-- Program -->
                <li>
                    <a href="@Url.Action("HODManagement", "AcademicSetup")"
                       class="@(activeSubMenu == "Program" ? "active-link" : "")"
                       asp-controller="Academic" asp-action="Program">
                        <i class="fas fa-graduation-cap"></i> HOD Management
                    </a>
                </li>
                <li>
                    <a href="@Url.Action("" +
                            "ProgramManagement", "AcademicSetup")"
                       class="@(activeSubMenu == "Program" ? "active-link" : "")"
                       asp-controller="Academic" asp-action="Program">
                        <i class="fas fa-graduation-cap"></i> Program
                    </a>
                </li>
                <!-- Course -->
                <li>
                    <a href="@Url.Action("CourseManagement", "AcademicSetup")"
                       class="@(activeSubMenu == "Course" ? "active-link" : "")"
                       asp-controller="Academic" asp-action="Course">
                        <i class="fas fa-book"></i> Course
                    </a>
                </li>
                <!-- Semester -->
                <li>
                    <a href="@Url.Action("SemesterManagement", "AcademicSetup")"
                       class="@(activeSubMenu == "Semester" ? "active-link" : "")"
                       asp-controller="Academic" asp-action="Semester">
                        <i class="fas fa-clock"></i> Semester
                    </a>
                </li>
                <!-- Subject -->
                <li>
                    <a href="@Url.Action("SubjectManagement", "AcademicSetup")"
                       class="@(activeSubMenu == "Subject" ? "active-link" : "")"
                       asp-controller="Academic" asp-action="Subject">
                        <i class="fas fa-puzzle-piece"></i> Subject
                    </a>
                </li>
            </ul>
        </li>

        <!-- ============================================================ -->
        <!-- SEAT MANAGEMENT -->
        <!-- ============================================================ -->
        <li>
            <div class="parent @(activeMenu == "Seat" ? "active" : "")" data-target="subSeat">
                <i class="fas fa-chair"></i>
                Seat Management
                <span class="arrow"><i class="fas fa-chevron-down"></i></span>
            </div>
            <ul class="submenu @(activeMenu == "Seat" ? "open" : "")" id="subSeat">
                <li>
                    <a asp-controller="Seat" asp-action="Matrix">
                        <i class="fas fa-table"></i> Seat Matrix
                    </a>
                </li>
                <li>
                    <a asp-controller="Seat" asp-action="Category">
                        <i class="fas fa-people-arrows"></i> Category-wise Seats
                    </a>
                </li>
                <li>
                    <a asp-controller="Seat" asp-action="Allotment">
                        <i class="fas fa-arrows-spin"></i> Seat Allotment
                    </a>
                </li>
            </ul>
        </li>

        <!-- ============================================================ -->
        <!-- ADMISSION -->
        <!-- ============================================================ -->
        <li>
            <div class="parent @(activeMenu == "Admission" ? "active" : "")" data-target="subAdmission">
                <i class="fas fa-user-graduate"></i>
                Admission
                <span class="arrow"><i class="fas fa-chevron-down"></i></span>
            </div>
            <ul class="submenu @(activeMenu == "Admission" ? "open" : "")" id="subAdmission">
                <li>
                    <a asp-controller="Admission" asp-action="Setup">
                        <i class="fas fa-gear"></i> Admission Setup
                    </a>
                </li>
                <li>
                    <a asp-controller="Admission" asp-action="StudentRegistration">
                        <i class="fas fa-user-plus"></i> Student Registration
                    </a>
                </li>
                <li>
                    <a asp-controller="Admission" asp-action="Application">
                        <i class="fas fa-file-lines"></i> Application Management
                    </a>
                </li>
                <li>
                    <a asp-controller="Admission" asp-action="DocumentVerification">
                        <i class="fas fa-check-double"></i> Document Verification
                    </a>
                </li>

                <li>
                    <a asp-controller="Admission" asp-action="Counselling">
                        <i class="fas fa-handshake"></i> Counselling
                    </a>
                </li>
                <li>
                    <a asp-controller="Admission" asp-action="Final">
                        <i class="fas fa-check-circle"></i> Admission (Final)
                    </a>
                </li>
            </ul>
        </li>

        <!-- ============================================================ -->
        <!-- STUDENT MASTER -->
        <!-- ============================================================ -->
        <li>
            <div class="parent @(activeMenu == "StudentMaster" ? "active" : "")" data-target="subStudentMaster">
                <i class="fas fa-user-graduate"></i>
                Student Master
                <span class="arrow"><i class="fas fa-chevron-down"></i></span>
            </div>
            <ul class="submenu @(activeMenu == "StudentMaster" ? "open" : "")" id="subStudentMaster">
                <li>
                    <a asp-controller="Student" asp-action="Records">
                        <i class="fas fa-id-card"></i> Student Records
                    </a>
                </li>
                <li>
                    <a asp-controller="Student" asp-action="Mapping">
                        <i class="fas fa-link"></i> Student Mapping
                    </a>
                </li>
                <li>
                    <a asp-controller="Student" asp-action="Identity">
                        <i class="fas fa-fingerprint"></i> Identity Generation
                    </a>
                </li>
                <li>
                    <a asp-controller="Student" asp-action="Progress">
                        <i class="fas fa-chart-line"></i> Academic Progress
                    </a>
                </li>
                <li>
                    <a asp-controller="Student" asp-action="Certificate">
                        <i class="fas fa-scroll"></i> Certificate Management
                    </a>
                </li>
                <li>
                    <a asp-controller="Student" asp-action="Alumni">
                        <i class="fas fa-users"></i> Alumni
                    </a>
                </li>
            </ul>
        </li>

        <!-- ============================================================ -->
        <!-- REPORTS -->
        <!-- ============================================================ -->
        <li>
            <div class="parent @(activeMenu == "Reports" ? "active" : "")" data-target="subReports">
                <i class="fas fa-chart-pie"></i>
                Reports
                <span class="arrow"><i class="fas fa-chevron-down"></i></span>
            </div>
            <ul class="submenu @(activeMenu == "Reports" ? "open" : "")" id="subReports">
                <li>
                    <a asp-controller="Reports" asp-action="Admission">
                        <i class="fas fa-print"></i> Admission Reports
                    </a>
                </li>
                <li>
                    <a asp-controller="Reports" asp-action="SeatAllotment">
                        <i class="fas fa-print"></i> Seat Allotment Report
                    </a>
                </li>
                <li>
                    <a asp-controller="Reports" asp-action="StudentMaster">
                        <i class="fas fa-print"></i> Student Master Report
                    </a>
                </li>
                <li>
                    <a asp-controller="Reports" asp-action="Academic">
                        <i class="fas fa-print"></i> Academic Reports
                    </a>
                </li>
            </ul>
        </li>

        <!-- ============================================================ -->
        <!-- MASTERS -->
        <!-- ============================================================ -->
        <li>
            <div class="parent @(activeMenu == "Masters" ? "active" : "")" data-target="subMasters">
                <i class="fas fa-database"></i>
                Masters
                <span class="arrow"><i class="fas fa-chevron-down"></i></span>
            </div>
            <ul class="submenu @(activeMenu == "Masters" ? "open" : "")" id="subMasters">
                <li>
                    <a href="@Url.Action("CategoryMaster", "Master")"
                       asp-controller="Master" asp-action="Category">
                        <i class="fas fa-tag"></i> Category Master
                        </a>
                </li>
                <li>
                    <a href="@Url.Action("NationalityMaster", "Master")"
                       asp-controller="Master" asp-action="Nationality">
                        <i class="fas fa-flag"></i> Nationality
                        </a>
                </li>
                <li>
                    <a href="@Url.Action("BoardUniversityMaster", "Master")"
                       asp-controller="Master" asp-action="Board">
                        <i class="fas fa-school"></i> Board / University
                    </a>
                </li>
                <li>
                    <a href="@Url.Action("CountryStateCityMaster", "Master")"
                       asp-controller="Master" asp-action="Location">
                        <i class="fas fa-building"></i> Country / State / City
                    </a>
                </li>
                <li>
                    <a href="@Url.Action("FeeheadMaster", "Master")"
                       asp-controller="Master" asp-action="FeeHeads">
                        <i class="fas fa-coins"></i> Fee Heads
                    </a>
                </li>
                <li>
                    <a href="@Url.Action("DiscountScholarshipMaster", "Master")"
                       asp-controller="Master" asp-action="Discount">
                        <i class="fas fa-percent"></i> Discount / Scholarship
                    </a>
                </li>
                <li>
                    <a href="@Url.Action("DesignationMaster", "Master")"
                       asp-controller="Master" asp-action="Designation">
                        <i class="fas fa-user-tie"></i> Designation
                    </a>
                </li>



                <li>
                    <a asp-controller="Master" asp-action="Campus">
                        <i class="fas fa-university"></i> Campus
                    </a>
                </li>


                <li>
                    <a asp-controller="Master" asp-action="ProgramType">
                        <i class="fas fa-layer-group"></i> Program Type
                    </a>
                </li>
                <li>
                    <a asp-controller="Master" asp-action="UniversityType">
                        <i class="fas fa-layer-group"></i> University Type
                    </a>
                </li>




                <li>
                    <a asp-controller="Master" asp-action="AdmissionMode">
                        <i class="fas fa-user-plus"></i> Admission Mode
                    </a>
                </li>

                <li>
                    <a asp-controller="Master" asp-action="CourseCodeDegree">
                        <i class="fas fa-graduation-cap"></i> Course Code / Degree
                    </a>
                </li>
                <li>
                    <a asp-controller="Master" asp-action="ParentSession">
                        <i class="fas fa-calendar-week"></i> Parent Session
                    </a>
                </li>

                <li>
                    <a asp-controller="Master" asp-action="SessionType">
                        <i class="fas fa-calendar-day"></i> Session Type
                    </a>
                </li>

                <li>
                    <a asp-controller="Master" asp-action="AcademicSession">
                        <i class="fas fa-calendar-alt"></i> Academic Session
                    </a>
                </li>

                <li>
                    <a asp-controller="Master" asp-action="SystemSettings">
                        <i class="fas fa-cog"></i> System Settings
                    </a>
                </li>
            </ul>
        </li>

    </ul>

    <!-- Sidebar Footer -->
    <div class="sidebar-footer">
        <span class="status-dot online"></span>
        <span>System Online</span>
        <span style="margin-left: auto; font-size: 0.65rem;">v2.0</span>
    </div>
</aside>

<!-- Sidebar Overlay (for mobile) -->
<div class="sidebar-overlay" id="sidebarOverlay"></div>

<script>
    (function () {
        'use strict';

        // ----- DOM References -----
        const sidebar = document.getElementById('sidebar');
        const overlay = document.getElementById('sidebarOverlay');

        // ----- Toggle Submenu -----
        document.querySelectorAll('.parent[data-target]').forEach(function (parent) {
            parent.addEventListener('click', function (e) {
                e.stopPropagation();

                const targetId = this.getAttribute('data-target');
                const submenu = document.getElementById(targetId);
                if (!submenu) return;

                // Toggle open class
                submenu.classList.toggle('open');

                // Rotate arrow
                const arrow = this.querySelector('.arrow i');
                if (arrow) {
                    arrow.style.transform = submenu.classList.contains('open')
                        ? 'rotate(180deg)'
                        : 'rotate(0deg)';
                }

                // Update active state
                document.querySelectorAll('.parent').forEach(function (p) {
                    p.classList.remove('active');
                });
                this.classList.add('active');
            });
        });

        // ----- Submenu Link Click -----
        document.querySelectorAll('.submenu li a').forEach(function (link) {
            link.addEventListener('click', function (e) {
                // Remove active from all submenu links
                document.querySelectorAll('.submenu li a').forEach(function (l) {
                    l.classList.remove('active-link');
                });
                this.classList.add('active-link');

                // Highlight parent
                const parentLi = this.closest('li')?.parentElement?.previousElementSibling;
                if (parentLi && parentLi.classList.contains('parent')) {
                    document.querySelectorAll('.parent').forEach(function (p) {
                        p.classList.remove('active');
                    });
                    parentLi.classList.add('active');

                    // Ensure submenu is open
                    const targetId = parentLi.getAttribute('data-target');
                    if (targetId) {
                        const sub = document.getElementById(targetId);
                        if (sub) {
                            sub.classList.add('open');
                            const arrow = parentLi.querySelector('.arrow i');
                            if (arrow) arrow.style.transform = 'rotate(180deg)';
                        }
                    }
                }

                // Close mobile sidebar after navigation
                if (window.innerWidth <= 992) {
                    closeSidebar();
                }
            });
        });

        // ----- Parent click (non-dropdown) -----
        document.querySelectorAll('.parent:not([data-target])').forEach(function (parent) {
            parent.addEventListener('click', function () {
                document.querySelectorAll('.parent').forEach(function (p) {
                    p.classList.remove('active');
                });
                this.classList.add('active');

                // Close mobile sidebar
                if (window.innerWidth <= 992) {
                    closeSidebar();
                }
            });
        });

        // ----- Open/Close Sidebar (Mobile) -----
        function openSidebar() {
            sidebar.classList.add('open');
            overlay.classList.add('active');
            document.body.style.overflow = 'hidden';
        }

        function closeSidebar() {
            sidebar.classList.remove('open');
            overlay.classList.remove('active');
            document.body.style.overflow = '';
        }

        // Expose for navbar hamburger
        window.toggleSidebar = function () {
            if (sidebar.classList.contains('open')) {
                closeSidebar();
            } else {
                openSidebar();
            }
        };

        // Close on overlay click
        if (overlay) {
            overlay.addEventListener('click', closeSidebar);
        }

        // Close on Escape key
        document.addEventListener('keydown', function (e) {
            if (e.key === 'Escape' && sidebar.classList.contains('open')) {
                closeSidebar();
            }
        });

        // Handle window resize
        let resizeTimer;
        window.addEventListener('resize', function () {
            clearTimeout(resizeTimer);
            resizeTimer = setTimeout(function () {
                if (window.innerWidth > 992 && sidebar.classList.contains('open')) {
                    closeSidebar();
                }
            }, 250);
        });

        // ----- Initialize: Open submenu if active -----
        document.querySelectorAll('.submenu.open').forEach(function (sub) {
            const parent = sub.parentElement?.querySelector('.parent');
            if (parent) {
                const arrow = parent.querySelector('.arrow i');
                if (arrow) arrow.style.transform = 'rotate(180deg)';
            }
        });

        // ----- Set active parent for active submenu links -----
        document.querySelectorAll('.submenu li a.active-link').forEach(function (link) {
            const parentLi = link.closest('li')?.parentElement?.previousElementSibling;
            if (parentLi && parentLi.classList.contains('parent')) {
                parentLi.classList.add('active');
                const targetId = parentLi.getAttribute('data-target');
                if (targetId) {
                    const sub = document.getElementById(targetId);
                    if (sub) {
                        sub.classList.add('open');
                        const arrow = parentLi.querySelector('.arrow i');
                        if (arrow) arrow.style.transform = 'rotate(180deg)';
                    }
                }
            }
        });

    })();
</script>